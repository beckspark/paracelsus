# Act (local GitHub Actions) secrets/env file
# Copy to .github/act/.env: cp .github/act/.env.example .github/act/.env
#
# Usage: ./scripts/run-elt.sh [job]
#
# These are demo credentials matching the docker-compose setup.

# =============================================================================
# Secrets (used by workflow via ${{ secrets.* }})
# =============================================================================

# Database passwords (match docker-compose .env)
OLTP_PASSWORD=oltp_secret
OLAP_PASSWORD=olap_secret

# AWS credentials (LocalStack accepts any value, 'test' is conventional)
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# HubSpot API token (mock server accepts any value)
HUBSPOT_ACCESS_TOKEN=mock-token
